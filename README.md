# MasterCompany
REST API built with .NET Core for employee management system. Implements CRUD operations with file-based storage (JSON/TXT), salary calculations, and gender analytics. Features layered architecture, LINQ queries, and Swagger documentation.
